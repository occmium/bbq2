<h1>События</h1>

<div class="buffer-both">
  <% if policy(Event).new? %>
    <%= link_to 'Создать событие', new_event_path, class: 'btn btn-success' %>
  <% else %>
    <%= link_to 'Желаете создать событие?', new_event_path, class: 'btn btn-success' %>
    <%#= link_to 'Желаете создать событие?', new_user_registration_path, class: 'btn btn-success' %>
  <% end %>
</div>

<div class="row buffer-bottom">
  <%= render partial: 'events/event', collection: @events %>
</div>
